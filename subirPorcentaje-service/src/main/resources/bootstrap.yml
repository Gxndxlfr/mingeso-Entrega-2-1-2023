spring:
  devtools:
    restart:
      aditional-paths: src/main/java
  application:
    name: subirPorcentaje-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  jpa:
    show-sql: true
    hibernate:
      dialect: PostgreSQLDialect
      ddl-auto: create-drop
  datasource:
    driver-class-name: org.postgresql.Driver
    dbname: postgres
    url: jdbc:postgresql://localhost:5432/SubirPorcentajeBD
    username: postgres
    password: admin




